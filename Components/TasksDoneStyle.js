import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    tasksDone:{
        color: '#FAEBD7',
        textAlign: 'center',
        fontSize: 25,
        paddingTop: '5%'
    }

});
export default styles;