import { Text, View } from 'react-native';
import React from 'react';
import styles from '../Components/upNextStyle';

export default function UpNext() {
    return(
        <View>
            <Text>Your Next Task</Text>
        </View>

    );
}